export type IOptions = {
  rootPath?: string;
  skipDownload?: true;
};

export type IUpdate = {
  seasonName: string;
  seasonPath: string;
  episodeName: string;
  episodePath: string;
  episodeUrl: string;
};
